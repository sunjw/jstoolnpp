function test(input) {
    var sql =
        `SELECT *
        FROM my_table
        LIMIT 1;`;
}

function test(input) {
    var sql =
    "SELECT *
                FROM my_table
        LIMIT 1;";
}